<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoofmemories.project.mapper.NoticeMapper">

    <resultMap id="NoticeWithPublisherResult" type="com.echoofmemories.project.entity.Notice">
        <id property="id" column="id"/>
        <result property="type" column="type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="publisherId" column="publisher_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
        <result property="publisherNickname" column="publisher_nickname"/>
    </resultMap>

    <select id="selectNoticePageWithPublisher" resultMap="NoticeWithPublisherResult">
        SELECT 
            n.*,
            u.nickname AS publisher_nickname
        FROM notice n
        LEFT JOIN sys_user u ON n.publisher_id = u.id
        WHERE n.deleted = 0
        <if test="status != null">
            AND n.status = #{status}
        </if>
        <if test="type != null">
            AND n.type = #{type}
        </if>
        ORDER BY n.create_time DESC
    </select>

    <select id="selectNoticeWithPublisherById" resultMap="NoticeWithPublisherResult">
        SELECT 
            n.*,
            u.nickname AS publisher_nickname
        FROM notice n
        LEFT JOIN sys_user u ON n.publisher_id = u.id
        WHERE n.id = #{id} AND n.deleted = 0
    </select>

</mapper>

